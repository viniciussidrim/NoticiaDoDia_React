import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
    box: {
        backgroundColor: '#000',
        alignItems: 'center',
    },
    texto:{
        fontSize: 28,
        padding: 10,
        color: 'white',
    }
});

export default estilo;